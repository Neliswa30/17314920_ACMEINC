@model IEnumerable<PROG7311_Task2.Models.AcmeIncTask2db.Admins>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New Admin User</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserPassword)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserPassword)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserSurname">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserSurname">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserSurname">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
